# Нормализация окончаний строк — все текстовые файлы в LF.
# Устраняет ложные изменения при открытии в Cursor/VS Code на Windows.
* text=auto eol=lf

# Windows-скрипты оставляем в CRLF
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf
